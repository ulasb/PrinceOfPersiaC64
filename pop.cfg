# ld65 config — PRG $0801-$cffd with graphics windows around the VIC bank
MEMORY {
    ZP:   file = "", start = $0002, size = $00fa, type = rw, define = yes;
    RAM2: file = "", start = $0200, size = $0200, type = rw;
    MAIN: file = %O, start = $07ff, size = $3801, fill = yes;
    W1:   file = %O, start = $4000, size = $1c00, fill = yes;
    HOLE: file = %O, start = $5c00, size = $2400, fill = yes;
    W2:   file = %O, start = $8000, size = $4ffe, fill = no;
}
SEGMENTS {
    LOADADDR: load = MAIN, type = ro;
    EXEHDR:   load = MAIN, type = ro;
    STARTUP:  load = MAIN, type = ro;
    CODE:     load = MAIN, type = ro,  optional = yes;
    RODATA:   load = MAIN, type = ro,  optional = yes;
    ANIMDATA: load = MAIN, type = ro,  optional = yes;
    DATA:     load = MAIN, type = rw,  optional = yes;
    BSS:      load = RAM2, type = bss, optional = yes, define = yes;
    ZEROPAGE: load = ZP,   type = zp,  optional = yes;
    GFX1:     load = W1,   type = ro;
    GFXH:     load = HOLE, type = ro;
    GFX2:     load = W2,   type = ro;
}
